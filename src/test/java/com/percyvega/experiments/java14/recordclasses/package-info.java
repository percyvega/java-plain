/**
 * Record classes are a simpler way of creating immutable classes.
 * Has an implicit @AllArgsConstructor.
 * All its fields are final and immutable.
 * It automatically generates equals(), hashCode(), and toString() methods.
 * Used to replace DTOs and POJOs.
 */
package com.percyvega.experiments.java14.recordclasses;
